{
  "id": "consera-contract-list-organizations-v1",
  "resource": "consera-contract-list-organizations",
  "tokenize": {
    "type": "JWT",
    "algorithm": "HS256",
    "encryption_key": "$ENV.CONTRACT_JWT_KEY"
  },
  "protected": {
    "provide_as": "Authorization",
    "extract_claims": ["id"]
  },
  "actionables": [
    {
      "id": 1,
      "mode": "QUERY",
      "payload_validation": [
        {
          "field": {
            "cursorId": {
              "type": "NUMBER"
            }
          }
        }
      ],
      "sql_query": {
        "query": "SELECT * FROM consera_contract.contract_list_organizations($1, $2)",
        "parameters": ["$PROTECTED.id", "$PAYLOAD.cursorId"]
      },
      "returnables": ["organization_id", "label"]
    }
  ],
  "returnables": ["organization_id", "label"]
}
