{
  "id": "consera-contract-claim-unique-v1",
  "resource": "consera-contract-claim-unique",
  "tokenize": {
    "type": "JWT",
    "algorithm": "HS256",
    "encryption_key": "$ENV.CONTRACT_JWT_KEY"
  },
  "protected": {
    "provide_as": "Authorization",
    "extract_claims": ["id"]
  },
  "actionables": [
    {
      "id": 1,
      "mode": "QUERY",
      "payload_validation": [
        {
          "field": {
            "organizationId": {
              "type": "NUMBER"
            }
          }
        },
        {
          "field": {
            "claimKey": {
              "type": "STRING"
            }
          },
          "min_length": 8,
          "max_length": 120
        }
      ],
      "sql_query": {
        "query": "SELECT * FROM consera_contract.contract_claim_unique($1, $2, $3)",
        "parameters": ["$PROTECTED.id", "$PAYLOAD.organizationId", "$PAYLOAD.claimKey"]
      },
      "returnables": ["claim_state"]
    }
  ],
  "returnables": ["claim_state"]
}
