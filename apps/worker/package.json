{
  "name": "@consera/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@consera/ai-provider": "workspace:*",
    "@consera/test-fixtures": "workspace:*"
  }
}
